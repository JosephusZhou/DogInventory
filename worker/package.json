{
  "name": "dog-inventory-share",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute dog_inventory_share --file=schema.sql",
    "db:init:local": "wrangler d1 execute dog_inventory_share --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240919.0",
    "wrangler": "^3.78.0",
    "typescript": "^5.6.0"
  }
}
